providers = ["node", "staticfile"]

[variables]
PORT = "3100"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "cd backend && npm install",
  "cd frontend && npm install && npm run build"
]
